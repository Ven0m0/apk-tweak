[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "revanced-pipeline"
version = "0.0.1"
description = "Extensible pipeline for ReVanced, Magisk modules, LSPatch, and DTL-X"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
rvp = "rvp.cli:main"
